load("@rules_cc//cc:defs.bzl", "cc_library")

# Windows only mmap implementation, nop otherwise..
cc_library(
    name = "mman",
    srcs =
        select({
            "@platforms//os:windows": ["mman.c"],
            "//conditions:default": [],
        }),
    hdrs = ["include/sys/mman.h"],
    includes = ["include"],
    tags = ["no-tidy"],
    visibility = ["//visibility:public"],
)
