load("@rules_cc//cc:defs.bzl", "cc_library", "cc_test")
load("//emulator/tools:plugins.bzl", "collect_plugins")

cc_library(
    name = "guest-status",
    srcs = ["guest_status_device.cc"],
    hdrs = ["include/goldfish/devices/guest_status/guest_status_device.h"],
    includes = ["include"],
    visibility = ["//visibility:public"],
    deps = [
        "//emulator/libs/async:async_api",
        "//emulator/libs/eventing",
        "//emulator/libs/system",
        "//emulator/plugin/avd_universe:grpc_notification",
        "//emulator/plugin/avd_universe:guest_status",
        "//emulator/plugin/hal/common:emulator_reset",
        "//emulator/plugin/hal/connector:connector_registry",
        "//emulator/plugin/hal/connector:qemud",
        "//emulator/plugin/vminterface",
        "@abseil-cpp//absl/log",
        "@abseil-cpp//absl/log:check",
        "@abseil-cpp//absl/strings",
        "@abseil-cpp//absl/strings:str_format",
        "@abseil-cpp//absl/synchronization",
    ],
    alwayslink = True,
)

collect_plugins(
    name = "binplace",
    output_dir = ".",
    plugins = [
        "@glib//glib:glib_shared",
        "@glib//gmodule:gmodule_shared",
    ],
)

cc_test(
    name = "guest-status_unittests",
    srcs = ["guest_status_device_test.cc"],
    data = [":binplace"],
    dynamic_deps = [
        "@glib//glib:glib_shared",
        "@glib//gmodule:gmodule_shared",
    ],
    includes = ["test"],
    visibility = ["//visibility:public"],
    deps = [
        ":guest-status",
        "//emulator/libs/async:async_testing",
        "//emulator/libs/system:test-headers",
        "//emulator/libs/test:googletest_main",
        "//emulator/plugin/hal/connector:test-helpers",
        "//emulator/plugin/hal/plug:fake_vsock",
        "//emulator/plugin/vminterface:vminterface-test",
        "@glib//glib:glib_import",
        "@glib//gmodule:gmodule_import",
        "@grpc//:grpc++",
    ],
)
