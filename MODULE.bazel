# This file is a placeholder and ignored by Bazel when this module is used via
# a registry. The "real" file lives at
# build/bazel/registry/modules/goldfish/0.0.1/MODULE.bazel.
bazel_dep(name = "rules_python", version = "1.7.0")
